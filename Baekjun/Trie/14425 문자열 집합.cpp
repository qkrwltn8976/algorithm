#include <iostream>
using namespace std;

int main()
{
    cin.tie(0);
    cout.tie(0);
    int n, m;

    cin >> n >> m;
    while(n--)
    {
        
    }
    while(m--)
    {

    }
    return 0;
}