/**
 * 단어 길이 재기
*/

#include <iostream>
#include <string>

using namespace std;

int main()
{
    cin.tie(0);
    cout.tie(0);
    string s;
    cin >> s;
    cout << s.length() << "\n";

    return 0;
}