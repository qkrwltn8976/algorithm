/**
 * 트리의 지름
*/

#include <iostream>
#include <vector>

using namespace std;

vector<int> tree;
int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    int V;
    cin >> V;

    while (V--)
    {
        int n;
        cin >> n;
        while (n != -1)
        {
            // 정점번호 + 정점까지의 거리
            cin >> n;
        }
    }

    return 0;
}